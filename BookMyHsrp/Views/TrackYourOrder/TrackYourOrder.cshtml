<html>


<head>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=0">

    <!-- Place favicon.ico in the root directory -->
    <link rel="shortcut icon" type="image/x-icon" href="Images/favicon.html">
    <link rel="apple-touch-icon" href="apple-touch-icon.html">

    <!-- All css files are included here. -->
    <link rel="stylesheet" href="~/css/bootstrap.min.css">
    <link rel="stylesheet" href="~/css/style.css">
    <link rel="stylesheet" href="~/css/owl.carousel.min.css">

</head>

<body>

        <!-- main start -->
        <main class="main-body bg-img pt--50 pb--80 main-bg">
            <div class="container">
                <div class="row justify-content-center">

                <div class="col-lg-4">
                        <div class="status-card" id="main">
                            <div class="title-area">
                                <h4 class="title">Track Your Order</h4>
                            </div>
                            <div class="card-body">
                                <form action="">
                                    <div class="row">
                                        <div class="col-lg-12">
                                            <div class="form-group">
                                                <label class="form-label">Order number*</label>
                                                <input type="text" class="form-control" placeholder="DLHSRP1221XXXXXX" id="orderNo">
                                            </div>
                                        </div>
                                        <div class="col-lg-12">
                                            <div class="form-group">
                                                <label class="form-label">Vehicle Reg No.*</label>
                                            <input type="text" class="form-control" placeholder="DL01ABXXXX" id="vehRegNo">
                                            </div>
                                        </div>
                                        <div class="col-lg-6">
                                            <div class="input-group">
                                                <label class="form-label w-100">Captcha</label>
                                            <input type="text" class="form-control" aria-label="Amount (to the nearest dollar)" id="txtCaptcha" readonly>
                                            <button class="input-group-text" id="btnRefreshCaptcha"><i class="fas fa-redo-alt"></i></button>
                                            </div>
                                        </div>
                                        <div class="col-lg-6">
                                            <div class="input-group">
                                                <label class="form-label w-100">Input Captcha</label>
                                            <input type="text" class="form-control" id="txtInput">
                                            </div>
                                        </div>
                                        <div class="col-lg-12">
                                        <button type="button" class="submit-btn" id="submit-button">
                                                Search
                                            </button>
                                            <button type="button" class="submit-btn">
                                                Cancel
                                            </button>
                                        </div>
                                    </div>
                                </form>
                            </div>
                        </div>
                </div>
                </div>
            <div class="row justify-content-center">
                <div class="col-lg-6">
                    <div class="status-card" id="hidden">
                        <div class="title-area">
                            <h4 class="title">Appointment Status</h4>
                        </div>
                        <div class="card-body">
                            <form action="">
                                <div class="row">
                                    <div class="col-lg-6 col-sm-12">
                                        <div class="d-desc">
                                            <label>Order Number</label>
                                            <p id="HiddenOrderNo"></p>
                                        </div>
                                    </div>
                                    <div class="col-lg-6 col-sm-12">
                                        <div class="d-desc">
                                            <label>Vehicle Reg. Number</label>
                                            <p id="vehicleNo"></p>
                                        </div>
                                    </div>
                                    <div class="col-lg-6 col-sm-12">
                                        <div class="d-desc">
                                            <label>Appointment Date</label>
                                            <p id="appointmentDate"></p>
                                        </div>
                                    </div>
                                    <div class="col-lg-6 col-sm-12">
                                        <div class="d-desc">
                                            <label>Order Status</label>
                                            <p id="orderstatus"></p>
                                        </div>
                                    </div>
                                    <div class="col-lg-6 col-sm-12">
                                        <div class="d-desc">
                                            <label>Name</label>
                                            <p id="ownername"></p>
                                        </div>
                                    </div>
                                    <div class="col-lg-6 col-sm-12">
                                        <div class="d-desc">
                                            <label>Fitment Address</label>
                                            <p id="address"></p>
                                        </div>
                                    </div>

                                    <div class="col-lg-12 mt-5">
                                        <button type="button" class="submit-btn" id="btnclose">
                                            Close
                                        </button>
                                            <button type="button" class="submit-btn" id="btndownloadreceipt">
                                            Download Receipt
                                        </button>
                                    </div>
                                </div>
                            </form>
                        </div>
                    </div>
                </div>
                </div>
            <div class="row justify-content-center">
                <div class="col-lg-6">
                    <div class="status-card" id="hidden1">
                        <div class="card-body">
                            <form action="">
                                <div class="row">
                                    <div class="col-lg-6 col-sm-12">
                                        <div class="d-desc">
                                            <label>Order Number</label>
                                            <p id="hiddenOrderNo1"></p>
                                        </div>
                                    </div>
                                    <div class="col-lg-6 col-sm-12">
                                        <div class="d-desc">
                                            <label>Vehicle Reg. Number</label>
                                            <p id="hiddenvehicleNo"></p>
                                        </div>
                                    </div>
                                    <div class="col-lg-6 col-sm-12">
                                        <div class="d-desc">
                                            <label>Previous Date</label>
                                            <p id="previousappointmentdate"></p>
                                        </div>
                                    </div>
                                    <div class="col-lg-6 col-sm-12">
                                        <div class="d-desc">
                                            <label>Reschedule Date</label>
                                            <p id="hiddenresceduledate"></p>
                                        </div>
                                    </div>
                                    <div class="col-lg-6 col-sm-12">
                                        <div class="d-desc">
                                            <label>Final Appointment Date</label>
                                            <p id="hiddenresceduleondate"></p>
                                        </div>
                                    </div>
                                    <div class="col-lg-6 col-sm-12">
                                        <div class="d-desc">
                                            <label>Order Status</label>
                                            <p id="hiddenorderstatus"></p>
                                        </div>
                                    </div>
                                    <div class="col-lg-6 col-sm-12">
                                        <div class="d-desc">
                                            <label>Name</label>
                                            <p id="hiddenownername"></p>
                                        </div>
                                    </div>
                                    <div class="col-lg-6 col-sm-12">
                                        <div class="d-desc">
                                            <label>Fitment Address</label>
                                            <p id="hiddenaddress"></p>
                                        </div>
                                    </div>
                                    <div class="col-lg-12 mt-5">
                                        <button type="button" class="submit-btn" id="btnclose1">
                                            Close
                                        </button>
                                        <button type="button" class="submit-btn" id="btndownloadreceipt1">
                                            Download Receipt
                                        </button>
                                    </div>


                                </div>
                            </form>
                        </div>
                    </div>

                </div>
                <div class="col-lg-6">
                    <div class="status-card" id="hidden2" style="height: 100%">
                        <div class="card-body">
                            <form action="">
                                <div class="row" id="reschedulediv">
                                       
                                </div>
                            </form>
                        </div>
                    </div>

                </div>
            </div>
        </div>
    </main>
       
 @section Scripts {
        <script>

            function DrawCaptcha() {
                var a = Math.ceil(Math.random() * 9) + '';
                var b = Math.ceil(Math.random() * 9) + '';
                var c = Math.ceil(Math.random() * 9) + '';
                var d = Math.ceil(Math.random() * 9) + '';
                var code = a + ' ' + b + ' ' + c + ' ' + d;
                document.getElementById("txtCaptcha").value = code;
            }

            window.onload = function () {
                $('#hidden').hide();
                $('#hidden1').hide();
                $('#hidden2').hide();
                            DrawCaptcha();
            document.getElementById("btnRefreshCaptcha").addEventListener("click", DrawCaptcha);
            };



                $('#btndownloadreceipt').click(function () {
                    handleButtonClickApproved.call(this, '/Receipt/DownloadReceipt', downloadReceipt, (result) => {
                        // Success callback
                        // You can handle the success result here, if needed
                    });
                });

                async function handleButtonClickApproved(url, downloadReceipt, isSuccessFunc) {
                    debugger;

                    var orderNumber = $('#orderNo').val();

                    $(this).prop('disabled', true);
                    var originalButtonText = $(this).html();

                    const errorFunc = (title) => {
                        Toast.fire({
                            icon: 'error',
                            title: title
                        });
                        $(this).prop('disabled', false);
                        $(this).html(originalButtonText);
                    };

                    const data = { OrderNo: orderNumber }
                    // AJAX request to download receipt
                    try {
                        const response = await downloadReceipt(url, data); // Assuming you have defined postData function
                        if (response.size > 15) {
                            isSuccessFunc(response);
                            debugger;
                            console.log(response);
                            const orderNo = response.OrderNo;

                            var blob = new Blob([response], { type: 'application/pdf' });
                            var url = window.URL.createObjectURL(blob);

                            var a = document.createElement('a');
                            a.href = url;
                            a.download = orderNumber + '.pdf';
                            document.body.appendChild(a);
                            a.click();

                            window.URL.revokeObjectURL(url);
                        }
                        else {
                            errorFunc('Invalid Order No.');
                        }
                    } catch (error) {
                        errorFunc('Error downloading receipt.');
                    }
                }


                $('#btndownloadreceipt1').click(function () {
                    handleButtonClickApproved.call(this, '/Receipt/DownloadReceipt', downloadReceipt, (result) => {
                        // Success callback
                        // You can handle the success result here, if needed
                    });
                });

                async function handleButtonClickApproved(url, downloadReceipt, isSuccessFunc) {
                    debugger;

                    var orderNumber = $('#orderNo').val();

                    $(this).prop('disabled', true);
                    var originalButtonText = $(this).html();

                    const errorFunc = (title) => {
                        Toast.fire({
                            icon: 'error',
                            title: title
                        });
                        $(this).prop('disabled', false);
                        $(this).html(originalButtonText);
                    };

                    const data = { OrderNo: orderNumber }
                    // AJAX request to download receipt
                    try {
                        const response = await downloadReceipt(url, data); // Assuming you have defined postData function
                        if (response.size > 15) {
                            isSuccessFunc(response);
                            debugger;
                            console.log(response);
                            const orderNo = response.OrderNo;

                            var blob = new Blob([response], { type: 'application/pdf' });
                            var url = window.URL.createObjectURL(blob);

                            var a = document.createElement('a');
                            a.href = url;
                            a.download = orderNumber + '.pdf';
                            document.body.appendChild(a);
                            a.click();

                            window.URL.revokeObjectURL(url);
                        }
                        else {
                            errorFunc('Invalid Order No.');
                        }
                    } catch (error) {
                        errorFunc('Error downloading receipt.');
                    }
                }

            function ValidCaptcha() {
                $(this).prop('disabled', true);

                const errorFunc = (title) => {
                    Toast.fire({
                        icon: 'error',
                        title: title
                    })



                }
                var captcha = removeSpaces(document.getElementById('txtCaptcha').value);
                var inputCaptcha = removeSpaces(document.getElementById('txtInput').value);
                if (inputCaptcha == '') {
                    //alert("Captcha can't be blank!!");
                    errorFunc("Captcha can't be blank!!");
                    return;
                }
                else if (captcha == inputCaptcha) {
                    return true;
                }
                else {
                    // alert("Please Input Correct Captcha");
                    errorFunc("Please Input Correct Captcha");
                    return;
                }
            }
            function removeSpaces(string) {
                return string.split(' ').join('');
            }

            function removeSpaces(string) {
                return string.replace(/\s+/g, '');
        }
            $('#submit-button').click(function () {
                handleButtonClick.call(this, '/track-your-order',postData, (result) => {
                    // bindHtmlHsrpView(result.Data);
                    // $('#div-bind-report').show();
                    // console.log('Data posted successfully, where the server response data is', result);
                })
            });

            async function handleButtonClick(url, postData, isSuccessFunc) {

                const errorFunc = (title) => {
                    Toast.fire({
                        icon: 'error',
                        title: title
                    })
                    $(this).prop('disabled', false);

                }
                var vehRegNo = $('#vehRegNo').val();
                var orderNo = $('#orderNo').val();
                if (!vehRegNo) {
                    errorFunc('Please Insert Registration No.')

                }
                if (!ValidCaptcha()) {
                    $(this).prop('disabled', false);
                    $(this).html(originalButtonText)
                    return false;
                }
                var data = { VehicleregNo: vehRegNo, OrderNo: orderNo }
                const result = await postData(url, data);
                if (result == "Not Found" || result.Count == 0) {
                    return errorFunc("Data Not Found");
                }
                var data = { VehicleRegNo: vehRegNo, OrderNo: orderNo }
                var url = "/track-your-order-sp"
                const spresult = await postData(url, data)

                debugger
                if (result == "Not Found" || result.Count == 0) {
                    if (spresult == "Not Found" || spresult.Count == 0) {
                        $('.status-card').hide();
                        return errorFunc("Data Not Found");
                    }
                }
                else {
                    debugger
                    if (spresult[0].ReAppointmentStatus == "1") {
                        $('.status-card').hide();
                        $('#hidden1').show();
                        $('#hidden2').show();
                        $('#hiddenOrderNo1').text(result[0].ORDER_NUMBER);
                        $('#hiddenorderstatus').text(result[0].ORDER_NUMBER);
                        $('#hiddenvehicleNo').text(result[0].REG_NUMBER);
                        if (_AppointmentType == "Home") {
                            $('#hiddenownername').text(result[0].OwnerName)
                            var FullAddress = result[0].ShippingAddress1 + " " + result[0].ShippingAddress2 + " " + result[0].ShippingCity + " " + result[0].ShippingState + " " + result[0].ShippingPinCode;
                            return $('#hiddenaddress').text(FullAddress)
                        }
                        else {

                            var dealerid = result[0].dealerid.toString();

                            var data = { VehicleregNo: vehRegNo, OrderNo: orderNo, Dealerid: dealerid };
                            const _dealer = await postData("/dealer-name", data);

                            if (_dealer.length > 0) {
                                $('#hiddenownername').text(_dealer[0].DealerAffixationCenterName);
                                $('#hiddenaddress').text(_dealer[0].DealerAffixationCenterAddress);
                            }
                        }
                        for (var i = 0; i < spresult.length; i++) {
                            debugger
                            $('#previousappointmentdate').text(spresult[i].AppointmentDate);
                            $('#hiddenresceduledate').text(spresult[i].RescheduledAppointmentDate);
                            $('#hiddenresceduleondate').text(spresult[i].RescheduledAppointmentDate);
                            debugger
                            var divToUpdate = document.getElementById('reschedulediv');
                            var customHTML =
                                "<div class='row'>" +
                                "<div class='col-lg-6 col-sm-12'>" +
                                "<div class='d-desc'>" +
                                "<label>Rescheduled On</label>" +
                                "<p>" + spresult[i].CreationDate + "</p>" +
                                "</div>" +
                                "</div>" +
                                "<div class='col-lg-6 col-sm-12'>" +
                                "<div class='d-desc'>" +
                                "<label>" + spresult[i].OrderNo + "</label>" +
                                "<a href='" + spresult[i].ReceiptPath + "' target='_blank'>Download</a>" +
                                "</div>" +
                                "</div>" +
                                "</div>";

                            divToUpdate.innerHTML = customHTML;
                        }
                    }
                    else {
                        $('.status-card').hide();
                        $('#hidden').show();
                        $('#HiddenOrderNo').text(result[0].ORDER_NUMBER);
                        $('#vehicleNo').text(result[0].REG_NUMBER)

                        const fitmentdate = await postData("/fitment-date", data)

                        if (fitmentdate && fitmentdate.Count > 0) {
                            $('#appointmentDate').text("Fitment Date will be informed later via SMS and on Call")
                        }
                        else {

                            var datetimeString = result[0].SlotBookingDate;
                            var date = new Date(datetimeString);
                            var day = ('0' + date.getDate()).slice(-2);
                            var monthNames = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"];
                            var month = monthNames[date.getMonth()];
                            var year = date.getFullYear();
                            var formattedDate = day + '-' + month + '-' + year;
                            $('#appointmentDate').text(formattedDate);
                        }
                        $('#orderstatus').text(result[0].OrderStatus)
                        var _AppointmentType = result[0].AppointmentType;
                        if (_AppointmentType == "Home") {
                            $('#ownername').text(result[0].OwnerName)
                            var FullAddress = result[0].ShippingAddress1 + " " + result[0].ShippingAddress2 + " " + result[0].ShippingCity + " " + result[0].ShippingState + " " + result[0].ShippingPinCode;
                            return $('#address').text(FullAddress)
                        }
                        else {

                            var dealerid = result[0].dealerid.toString();

                            var data = { Dealerid: dealerid };
                            const _dealer = await postData("/dealer-name", data);

                            if (_dealer.length > 0) {
                                $('#ownername').text(_dealer[0].DealerAffixationCenterName);
                                return $('#address').text(_dealer[0].DealerAffixationCenterAddress);
                            }

                        }
                    }
                }
            }
        </script>

        }
        
</body>
</html>
